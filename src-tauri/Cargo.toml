[package]
name = "boltscribe"
version = "1.0.0"
description = "Local-first macOS and Windows voice input with ASR transcription and LLM correction"
authors = ["BoltScribe contributors"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
anyhow = "1.0"
arboard = "3.4"
base64 = "0.21"
chrono = { version = "0.4", features = ["serde"] }
cpal = "0.15"
dirs = "5.0"
flate2 = "1.1.9"
hound = "3.5"
reqwest = { version = "0.11", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tauri = { version = "2", features = ["macos-private-api", "tray-icon"] }
tauri-plugin-global-shortcut = "2.3.1"
tungstenite = { version = "0.29.0", features = ["native-tls"] }
uuid = { version = "1.6", features = ["v4", "serde"] }

[target.'cfg(target_os = "macos")'.dependencies]
core-foundation-sys = "0.8"
coreaudio-sys = "0.2"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.54", features = ["Win32_Foundation", "Win32_System_LibraryLoader", "Win32_UI_Input_KeyboardAndMouse", "Win32_UI_WindowsAndMessaging"] }
winreg = "0.50"

[features]
custom-protocol = ["tauri/custom-protocol"]
